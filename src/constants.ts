/**
 * Shared constants used across components.
 */

export const MONTHS = [
  "Jan", "Feb", "Mar", "Apr", "May", "Jun",
  "Jul", "Aug", "Sep", "Oct", "Nov", "Dec",
];

export const INPUT_CLASS = "input-base";

/** Fallback color for tags (e.g. tag pills, todate detail) when no tag color is set. */
export const DEFAULT_TAG_COLOR = '#9ca3af'; // gray-400

/** Fallback color for timeline bar items (brackets, lines) when no color is set. */
export const DEFAULT_TIMELINE_COLOR = '#6b7280'; // gray-500
