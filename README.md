update how the focus zooms when using
  - Update to match mobile best practices
don't require tag on item, always show things with no tags when filtering by tag
allow school year to use quarter, trimester, semester
Create modal to customize school year data.
  Start year
  Repeated grades
  gap years
  skipped grades
    - Utilize this data to calculate what years
  - Use FAB to create school data
  - then show minifab adjacent that is a schoolhouse and lets you edit the info
tag text to match all other text
make background of info on cards 'transparent' show background of list color. make text match contrast.
customize what's included in print screen and how multiple pages layout
add setting for card size, make them larger or smaller (maybe left to right option?)

Update to utilize a db, what's the best HIPPA compliant, low cost, least security vulnerability solution? Ideally I don't even have to stand something else up. Would users be needed or could we do session based and allow exporting/importing of data through hashed code. If we do start storing user based data Are we liable at all if I just mention that this data could be breached and shouldn't be confidential information when users go to save or something